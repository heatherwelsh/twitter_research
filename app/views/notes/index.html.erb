<%= form_for([@tweet, @note]) do |f| %>
  <fieldset>
    <legend>Note</legend>

    <div>
      <%= form.label(:notes_tex) %>
      <%= form.text_area(:note_text) %>
    </div>

    <div>
      <%= form.submit %>
    </div>
  <% end %>
